@model Test
@{ 
    int index = 0;
    string testName = Model.Name;
    string testCategory = Model.Category;
}
<div class="text-center bg-light">
    <form asp-action="ShowResultPage" method="post">
        <input type="hidden" asp-for="Name" />
        <input type="hidden" asp-for="Category" />
        <input type="hidden" asp-for="Count" />

        @foreach (var question in Model.Questions)
        {
            <input type="hidden" asp-for="Questions[index].ExpectedAnswer" value="@question.ExpectedAnswer" />
            <div class="form-group m-5 p-2">

                <div class="form-group">
                    <h2 class="form-control-plaintext">@question.Text</h2>
                    <img src="~/images/@testCategory/@testName/@(index + 1).png " class="form-control-file" />
                </div>

                <div class="collapse d-sm-inline-flex">
                    <div class="form-group m-3">
                        <label asp-for="Questions[index].ActualAnswer">А</label>
                        <input type="radio" asp-for="Questions[index].ActualAnswer" value="@AnswersList.Answers.A" class="form-control"/>
                    </div>

                    <div class="form-group m-3">
                        <label asp-for="Questions[index].ActualAnswer">Б</label>
                        <input type="radio" asp-for="Questions[index].ActualAnswer" value="@AnswersList.Answers.B" class="form-control" />
                    </div>

                    <div class="form-group m-3">
                        <label asp-for="Questions[index].ActualAnswer">В</label>
                        <input type="radio" asp-for="Questions[index].ActualAnswer" value="@AnswersList.Answers.C" class="form-control" />
                    </div>

                    <div class="form-group m-3">
                        <label asp-for="Questions[index].ActualAnswer">Г</label>
                        <input type="radio" asp-for="Questions[index].ActualAnswer" value="@AnswersList.Answers.G" class="form-control"/>
                    </div>

                    <div class="form-group m-3">
                        <label asp-for="Questions[index].ActualAnswer">Д</label>
                        <input type="radio" asp-for="Questions[index].ActualAnswer" value="@AnswersList.Answers.D" class="form-control" />
                    </div>
                </div>
                

            </div>
            index++;

        }
        <button>Завершити</button>
    </form>
</div>